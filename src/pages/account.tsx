import Typography from "@/components/ui/typography";
import Layout from "@/layouts/layout";

const AccountPage = () => {
  return (
    <Layout>
      <Typography title>Account</Typography>
      <Typography subtitle>Check out your account.</Typography>
    </Layout>
  );
};

export default AccountPage;
